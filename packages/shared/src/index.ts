// Placeholder — shared types/helpers land here as real slices need them.
// Emits CJS to dist/; rebuild (`pnpm --filter @pkos/shared build`) after edits.
export const SHARED_PACKAGE = '@pkos/shared'
